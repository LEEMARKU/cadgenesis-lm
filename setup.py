"""Setup shim for CADGenesis-LM.

Build configuration lives in ``pyproject.toml``. This file exists to support
legacy ``pip install -e .`` invocations and tools that still expect a
``setup.py``.
"""

from setuptools import setup

setup()

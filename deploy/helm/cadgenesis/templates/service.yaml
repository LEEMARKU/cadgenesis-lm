apiVersion: v1
kind: Service
metadata:
  name: {{ include "cadgenesis.fullname" . }}
  labels:
    {{- include "cadgenesis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cadgenesis.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.httpPort }}
      targetPort: http
    - name: grpc
      port: {{ .Values.service.grpcPort }}
      targetPort: grpc
